#! /usr/bin/env python
from subprocess import call

no_files = 3

for i in range(1,no_files):
    commandstring = "mv"
    mystring = " woa%i.txt" %(i)
    otherstring = " a%i.txt" %(i)
    call(commandstring + mystring + otherstring, shell=True)
